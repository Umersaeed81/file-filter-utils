from setuptools import setup, find_packages

setup(
    name="file-filter-utils",
    version="1.0.0",
    description="Flexible file filtering based on date extraction from filenames",
    author="Umer Saeed",
    packages=find_packages(),
    python_requires=">=3.7",
)
